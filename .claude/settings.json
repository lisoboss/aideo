{
  "permissions": {
    "allow": [
      "Bash(uv add *)",
      "Bash(uv run *)",
      "Bash(uv sync *)"
    ],
    "deny": [
      "Edit(*.pre-commit-config.yaml)",
      "Edit(*pyproject.toml)",
      "Write(*.pre-commit-config.yaml)",
      "Write(*pyproject.toml)"
    ]
  }
}
